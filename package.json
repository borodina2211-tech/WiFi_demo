{
  "name": "maxmsp-tap-bridge",
  "version": "1.0.0",
  "description": "WebSocket → UDP bridge for Max MSP participant tapping",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
